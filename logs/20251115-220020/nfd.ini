[general]
# (str) best-route / asf / multicast 
strategy = best-route
# (str) tcp / udp / ether
protocol = tcp
# (str) none / warn / info / debug / trace
logLevel = debug
# (bool) cli
cli = true

[cache]
# (int) capacity of cached segments
csSize = 256
# (str) Not Support, default lru
csPolicy = 
# (str) Not Support, default drop-all
csUnsolicitedPolicy = 

[suppression] # (int) miliseconds
# default: 10
retx-suppression-inital = 10
# default: 250
retx-suppression-max = 250
# default: 2
retx-suppression-multiplier = 2

